* Create boards from config
* Add function to fetch tickets from board
* Implement the scoring plugins
* Create a UI
* Create the API
* Build a custom strategy
* Add command to refresh in the background
